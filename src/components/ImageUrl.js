import React from "react";
import useFirestore from "../hooks/useFirestore";

const ImageUrl = () => {

    // const imagesUrl = useFirestore();

    return (
        <div>
            {/* {imagesUrl && <div>{imagesUrl[0]}</div>} */}
            <h1>check</h1>
        </div>
    )
}

export default ImageUrl;
